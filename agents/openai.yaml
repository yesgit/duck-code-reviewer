interface:
  display_name: "DuckDuckReviewer"
  short_description: "Structured review for code, diff, commit, PR/MR, and architecture changes"
  default_prompt: "Use $DuckDuckReviewer to review this code, diff, commit, PR/MR, or architecture change. Respond in the user's language unless they ask for another one."

policy:
  allow_implicit_invocation: true
